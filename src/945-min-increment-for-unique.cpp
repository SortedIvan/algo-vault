class Solution {
public:
    int minIncrementForUnique(vector<int>& nums) {

        if (nums.size() == 0) {
            return 0;
        }

        if (nums.size() == 1) {
            return 1;
        }

        std::sort(nums.begin(), nums.end());

        int amount = 0;

        for (int i = 1; i < nums.size(); ++i) {
            if (nums[i] == nums[])
        }

        return moves;
    }
};
